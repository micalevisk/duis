const path = require('path')
const fs = require('fs')

const { DUIS_CONFIG_FILENAME } = require('../../lib').constants

/**
 * @param {string} lookupDirAbsPath
 */
module.exports = function checkup(lookupDirAbsPath) {
  // TODO: percorrer todos os arquivos de lookup verificando se cada entry "resolvida" está atualizada
}
